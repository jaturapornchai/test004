# Product API Summary

## 📁 ไฟล์ที่สร้างขึ้น

1. **main.go** - ไฟล์หลักของ API server
2. **go.mod** - Go module configuration
3. **README.md** - คู่มือการใช้งาน
4. **mock_data.json** - ข้อมูลตัวอย่างในรูปแบบ JSON
5. **example_client.go** - ตัวอย่าง Go client
6. **test_api_en.ps1** - สคริปต์ทดสอบ API (PowerShell)
7. **test_api.sh** - สคริปต์ทดสอบ API (Bash)
8. **run.ps1** - สคริปต์รันเซิร์ฟเวอร์ (PowerShell)
9. **run.bat** - สคริปต์รันเซิร์ฟเวอร์ (Batch)

## 🚀 การรันโปรเจกต์

### วิธีที่ 1: รันด้วย PowerShell
```powershell
.\run.ps1
```

### วิธีที่ 2: รันด้วย Go command
```bash
go run main.go
```

### วิธีที่ 3: รันด้วย Batch file
```cmd
run.bat
```

## 🧪 การทดสอบ API

### ทดสอบด้วย PowerShell
```powershell
.\test_api_en.ps1
```

### ทดสอบด้วยตัวอย่าง Go Client
```bash
go run example_client.go
```

## 📋 API Endpoints

- **GET** `/api/products` - ดึงรายการสินค้าทั้งหมด
- **GET** `/api/products/:id` - ดึงข้อมูลสินค้าตาม ID
- **POST** `/api/products` - สร้างสินค้าใหม่
- **PUT** `/api/products/:id` - แก้ไขข้อมูลสินค้า
- **DELETE** `/api/products/:id` - ลบสินค้า
- **GET** `/api/categories` - ดึงรายการหมวดหมู่สินค้า
- **GET** `/api/health` - ตรวจสอบสถานะเซิร์ฟเวอร์

## 📊 Mock Data

API มาพร้อมกับข้อมูลสินค้าตัวอย่าง 6 รายการ:

1. **iPhone 15 Pro** - 39,900 บาท (Electronics)
2. **MacBook Air M2** - 35,900 บาท (Electronics)
3. **AirPods Pro 2** - 8,900 บาท (Electronics)
4. **Nike Air Max 270** - 4,500 บาท (Fashion)
5. **เสื้อยืดผ้าฝ้าย 100%** - 299 บาท (Fashion)
6. **กาแฟอาราบิก้าคั่วกลาง** - 350 บาท (Food & Beverage)

## 🔧 คุณสมบัติ

- ✅ RESTful API design
- ✅ JSON response format
- ✅ CORS support
- ✅ Data validation
- ✅ Error handling
- ✅ Filtering by category and price range
- ✅ Mock data included
- ✅ Health check endpoint

## 🌐 การเข้าถึง

เมื่อรันเซิร์ฟเวอร์แล้ว สามารถเข้าถึงได้ที่:
- **API Base URL**: http://localhost:8080/api
- **Documentation**: http://localhost:8080

ตัวอย่างการใช้งาน:
```bash
# ดึงสินค้าทั้งหมด
curl http://localhost:8080/api/products

# ดึงสินค้าในหมวด Electronics
curl http://localhost:8080/api/products?category=Electronics

# ดึงสินค้าในช่วงราคา 1000-10000 บาท
curl "http://localhost:8080/api/products?min_price=1000&max_price=10000"
```

API พร้อมใช้งานแล้ว! 🎉
