# 📋 Binance Trading Bot - Pure AI Trading System- **Leverage**: 10x (ทดสอบแล้ว ✅ ทำงานได้สมบูรณ์)
## 🎯 ภาพรวมระบบ
ระบบเทรดอัตโนมัติที่ใช้ AI เป็นหลักในการตัดสินใจเปิด/ปิด positions บน Binance Futures โดยไม่พึ่งพา SuperTrend หรือ EMA indicators

## ⚙️ กฎการพัฒนาและบำรุงรักษา
- ใช้ Golang เป็นหลัก
- ใช้ api เชื่อมต่อกับ Binance Official API
- Binance futures เท่านั้น (USDT pairs) ไม่ใช้ spot
- กระจาย file golang ใช้ import function ระหว่าง file ให้มากที่สุด
- ลบ file และ function ที่ไม่จำเป็นออกเสมอ
- การทดสอบทั้งหมดให้อยู่ใน folder test เท่านั้น
- ใช้ function จริงและข้อมูลจริงจาก Binance ในการทดสอบ (ไม่ใช้ mock/stub)
- ตรวจสอบความสมบูรณ์ของระบบก่อนทุกครั้ง ว่าไม่มี error
- ตอนจะทดสอบระบบ ให้ปิดทุกอย่างก่อน แล้วค่อยเปิดใหม่
- ข้อความ output (terminal) ให้เป็นภาษาไทยทั้งหมด
- ไม่ใช้ f-string formatting ที่ซับซ้อน ใช้ concatenate string ธรรมดา
- ห้ามแก้ไข folder markdown (เป็นต้นฉบับวิธีการทำงาน)
- ห้ามแก้ไข folder prompts (เป็น manual prompt files)
- ห้ามแก้ไขไฟล์ .env (config สำหรับการเชื่อมต่อ API)
- ไม่ต้องสร้าง .md file ใหม่ ใช้ไฟล์ markdown/step.md เป็นหลัก
- ใช้ try-except จัดการ error ทุกจุด ไม่ให้ระบบล่ม
- แสดง error message เป็นภาษาไทย
- ไม่ใช้ RSI ให้ตามลบ function และ class ที่เกี่ยวข้อง และ file ที่ไม่จำเป็นออก ทั้งหมด

## 📊 กฎสำคัญของระบบเทรด
### ❌ ข้อห้าม
- **ห้ามเปิด position ซ้ำในเหรียญเดียวกัน**
- **ห้ามเปิด position เพิ่ม หรือเปลี่ยนขนาด position** ถ้า position เปิดอยู่แล้ว
- **ห้ามตั้ง Stop Loss/Take Profit อัตโนมัติ** - เป็น AI-Only Mode

### ✅ ข้อบังคับ
- **ใช้ AI เป็นหลักในการตัดสินใจทั้งหมด** - ยกเลิก indicators อื่นทั้งหมด
- **Time Frame 1 ชั่วโมง เท่านั้น** - ใช้ข้อมูลราคา OHLCV สำหรับให้ AI วิเคราะห์
- **Pure AI Mode**: AI ตัดสินใจทุกครั้ง (ดู action เท่านั้น ไม่ดู confidence)

##  Trading Signal Conditions

### 🟢 AI-Only Strategy
**เงื่อนไข:** ถาม AI ทุกครั้งโดยใช้ข้อมูล OHLCV
1. **ดึงข้อมูล**: OHLCV 288 แท่งล่าสุด (1 ชั่วโมง)
2. **ส่งให้ AI**: วิเคราะห์ทิศทาง LONG/SHORT/HOLD

### 🔴 AI Decision Only  
**เงื่อนไข:** AI ตัดสินใจทุกครั้ง
1. **รับข้อมูล**: OHLCV 288 แท่ง (1 ชั่วโมง)
2. **ตัดสินใจ**: LONG/SHORT/HOLD พร้อม confidence %

## 📈 Position Parameters
- **Position Size**: 15 USDT ต่อ position
- **Leverage**: 10x (ทดสอบแล้ว ✅ ทำงานได้สมบูรณ์)
- **Margin Mode**: isolated (ใช้ค่า default จาก Binance ✅)
- **Total Position Value**: 150 USDT (15 USDT x 10x leverage)
- **ต้องมี Balance**: อย่างน้อย 16 USDT

## 🔄 ขั้นตอนการทำงานของระบบ

### เริ่มต้นโปรแกรม
```bash
go run app.go
```

### LOOP1: ตรวจสอบและจัดการ Positions (ทุก 1 ชั่วโมง)
1. ตรวจสอบ positions ที่เปิดอยู่ทั้งหมด
2. ดึง OHLCV 288 แท่งล่าสุด (1 ชั่วโมง) สำหรับแต่ละ position
3. ส่งข้อมูล position + OHLCV ให้ AI วิเคราะห์
4. AI ตัดสินใจ: CLOSE หรือ HOLD
5. ปิด position ถ้า AI แนะนำ CLOSE (ไม่ดู confidence)
6. ดึงรายชื่อเหรียญจาก Binance Futures (USDT pairs, สถานะ TRADING)

### LOOP2: สแกนหาโอกาสใหม่
1. วิเคราะห์เหรียญทีละตัวด้วย AI (เฉพาะเหรียญที่สถานะ TRADING บน Binance)
   - ดึง OHLCV 288 แท่ง (1 ชั่วโมง)
   - ส่งข้อมูลให้ AI วิเคราะห์
   - AI ตัดสินใจ LONG/SHORT/HOLD

2. **เงื่อนไขการเปิด position (AI Mode)**:
   - AI action เป็น LONG หรือ SHORT (ไม่ดู confidence)
   - ไม่มี position ซ้ำในเหรียญนั้น
   - มี balance เพียงพอ (≥ 16 USDT)

3. **ไม่ใช้ batch processing**: วิเคราะห์เหรียญทีละตัวแบบต่อเนื่อง (พัก 1 วินาที/เหรียญ)
4. วนจนครบทุกเหรียญหรือ balance หมด
5. รอจนถึงชั่วโมงถัดไป แล้วกลับไป LOOP1

## 🤖 AI Integration

### Pure AI System

ระบบใช้ AI เป็นหลักในการตัดสินใจทุกครั้ง:

#### 🤖 Pure AI Mode

- **เงื่อนไข**: ทุกสถานการณ์ใช้ AI ตัดสินใจ
- **การทำงาน**: ส่งข้อมูล OHLCV ให้ AI วิเคราะห์ทุกครั้ง
- **ข้อดี**: การวิเคราะห์แม่นยำ, ไม่พึ่งพา indicators
- **ตัดสินใจ**: ดู action เท่านั้น (LONG/SHORT/HOLD) ไม่ดู confidence

### Manual Prompt Mode
- **ไม่สร้าง prompt อัตโนมัติ** - ใช้ manual prompt ที่เขียนไว้
- **Prompt files**:
  - `prompts/open_position_prompt.txt` - สำหรับเปิด position
  - `prompts/close_position_prompt.txt` - สำหรับปิด position
- **ภาษา**: Prompt ภาษาไทย, Response ภาษาไทย
- **ไม่ต้องชี้นำ AI มาก** - ให้ข้อมูลแล้วให้ AI วิเคราะห์เอง

### AI Response Handling
- Response อาจไม่เป็น JSON เสมอไป
- ใช้ regex หา JSON format ใน response
- มี fallback เมื่อ parse JSON ไม่ได้
- แสดง AI confidence แม้ action จะเป็น HOLD

### AI Decision Points

1. **เปิด Position (Pure AI Mode)**:
   - รับข้อมูล: OHLCV 288 แท่ง (1 ชั่วโมง)
   - ตัดสินใจ: LONG/SHORT/HOLD (ดู action เท่านั้น ไม่ดู confidence)
   - AI วิเคราะห์ทุกอย่างเอง: ราคา, ปริมาณ, momentum, patterns

2. **ปิด Position (ใช้ AI เท่านั้น)**:
   - รับข้อมูล: OHLCV 288 แท่ง (1 ชั่วโมง) + position data
   - ตัดสินใจ: CLOSE/HOLD (ดู action เท่านั้น ไม่ดู confidence)
   - วิเคราะห์: โอกาสขาดทุน, รักษากำไร, หรือถือต่อ

## 📂 โฟลเดอร์สำคัญ
- `markdown/` - เอกสารต้นฉบับ (ห้ามแก้ไข)
- `prompts/` - Manual prompt files

## ⚠️ ข้อจำกัดที่ทราบ

- **Leverage Setting**: ✅ ทำงานได้สมบูรณ์ (ทดสอบแล้ว)
- **API Signature Error**: เกิดขึ้นบางครั้งตอนตั้ง margin mode แต่ไม่กระทบการทำงาน
- **AI response**: อาจช้าหรือ timeout (มี retry mechanism)
- **Balance Requirement**: ต้องมี balance อย่างน้อย 16 USDT ต่อ position
- **Position Value**: 15 USDT x 10x leverage = 150 USDT total exposure
- **การคัดกรองเข้มข้น**: ระบบจะเปิด position น้อยลงเพื่อความปลอดภัย

## 📊 การติดตามผล

- **Terminal Output**:
  - Balance และ PnL realtime
  - Position status ทุกชั่วโมง
  - AI confidence % ทุกการตัดสินใจ
  - AI decision results (LONG/SHORT/HOLD)
  - การวิเคราะห์ OHLCV แต่ละเหรียญ
- **Log Files**: ไม่มี (ดูจาก terminal เท่านั้น)
- **Position Tracking**: แสดง entry price, size, PnL

## 🔐 ความปลอดภัย

- ใช้ isolated margin ลดความเสี่ยง
- Position size เล็ก (15 USDT)
- ไม่มี martingale หรือ grid
- AI ตัดสินใจทุกครั้ง (ดู action เท่านั้น ไม่ดู confidence)
- Pure AI mode สำหรับความแม่นยำสูงสุด
